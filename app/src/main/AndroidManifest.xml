<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trulselg.myfirstapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_1"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DisplayMessageActivity" />
        <activity android:name=".ShowMessageActivity" />
        <activity android:name=".ResponseNoActivity" />
        <activity android:name=".ResponseYesActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".ShowLocaleActivity"></activity>
    </application>

</manifest>